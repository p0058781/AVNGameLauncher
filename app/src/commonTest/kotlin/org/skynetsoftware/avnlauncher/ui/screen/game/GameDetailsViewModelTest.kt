package org.skynetsoftware.avnlauncher.ui.screen.game

import java.time.ZonedDateTime
import kotlin.test.Test

class GameDetailsViewModelTest {
    @Test
    fun `test bla bla bla`() {
        val currentTimeMillis = System.currentTimeMillis()
        val millisDateTime = ZonedDateTime.now().toInstant().toEpochMilli()

        println(currentTimeMillis)
        println(millisDateTime)
    }
}
